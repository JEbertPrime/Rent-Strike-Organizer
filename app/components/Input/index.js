import styled from 'styled-components';
import { Input as input, Label as label } from 'reactstrap';

const Input = styled(input)`
  margin-bottom: 0.25em;
  margin-top: 0px;
  border-radius: 3px;
  padding-left: 0.7em;
`;
export const Label = styled(label)`

`
export default Input;
